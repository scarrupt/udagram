version: "3"
services:
  reverseproxy:
    build:
      context: .
    image: stevecarrupt/reverseproxy 
  backend_user:
    build:
      context: ../../api-user
    image: stevecarrupt/udagram-api-user
  backend_feed:
    build:
      context: ../../api-feed  
    image: stevecarrupt/udagram-api-feed
  frontend:
    build:
      context: ../../frontend  
    image: stevecarrupt/udagram-frontend:local
